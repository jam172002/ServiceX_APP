enum TransactionType { credit, debit }

enum TransactionCase { sent, received, cancelled }

/// UI Tabs enum
enum FixxerRequestTypeTab { all, direct, open }

/// Job data enum
enum FixxerJobRequestType { direct, open }
